Get the UID of given user.

# Install

    npm install getuid

# Usage

```javascript
var getuid = require('getuid');

getuid('username', function(err, uid) {
  console.log(uid);
});
```

# TODO

Sync version.
